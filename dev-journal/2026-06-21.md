# 2026-06-21 开发日志

## 本次目标

- 推进 M3：实现文字剪贴板监听。
- 支持窗口展示文字历史。
- 支持点击文字历史后重新写回系统剪贴板。
- 不实现本地存储、图片记录、置顶、删除、搜索、托盘、快捷键或开机自启。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 在 Electron 主进程中增加文字剪贴板轮询。
- 在主进程中以内存数组保存最多 50 条文字历史。
- 增加重复文字合并逻辑：相同文字再次出现时移动到顶部并更新时间。
- 增加 IPC 接口，用于获取文字历史和将指定文字记录写回剪贴板。
- 在 preload 中暴露 `window.clipboardHistory` 安全接口。
- 在窗口中渲染文字历史卡片。
- 支持点击文字卡片后重新写入系统剪贴板。
- 增加 M3 smoke 测试路径，验证文字写入、展示和点击写回。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run build`。
- 在允许 GUI 的环境中运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test`。
- 检查源码，确认未实现图片、本地存储、置顶、删除、搜索、托盘、快捷键或开机自启。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- 当前文字历史只保存在内存中，应用重启后会丢失，符合 M3 边界。

## 遗留问题

- 尚未实现本地持久化存储。
- 尚未实现图片记录。
- 尚未实现置顶、删除、搜索。
- 尚未实现保留时间清理。
- 尚未实现托盘、快捷键、开机自启。

## 下一步待办

- 等用户确认后，进入 M4：本地历史存储。
- M4 只处理本地保存、重复内容合并和按时间倒序读取。

## 备注

- Electron GUI smoke 测试需要清理 `ELECTRON_RUN_AS_NODE` 环境变量，并在允许 GUI 的环境中运行。

---

# M4 本地历史存储记录

## 本次目标

- 推进 M4：实现本地文字历史存储。
- 支持应用启动时读取本地文字历史。
- 支持重复文字合并并按时间倒序展示。
- 不实现图片记录、置顶、删除、搜索、保留时间清理、托盘、快捷键或开机自启。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 新增 `src/history-store.ts`，负责文字历史 JSON 文件读写。
- 将文字历史保存到 Electron `app.getPath("userData")` 目录下的 `text-history.json`。
- 应用启动时读取本地文字历史，并按 `copiedAt` 时间倒序排列。
- 保留 M3 的重复文字合并逻辑，并在每次历史变化后写入本地文件。
- 新增 M4 smoke 测试路径，使用临时 `userData` 目录验证持久化，不污染真实用户数据。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run build`。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 验证本地存储、重复合并和倒序读取。
- 检查源码，确认未实现图片、置顶、删除、搜索、保留时间清理、托盘、快捷键或开机自启。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M4 smoke 测试使用临时用户数据目录，测试结束后自动清理。

## 遗留问题

- 尚未实现图片记录。
- 尚未实现置顶、删除、搜索。
- 尚未实现保留时间清理。
- 尚未实现托盘、快捷键、开机自启。
- 本地文字历史当前为明文 JSON，符合第一版已确认范围，但不提供加密。

## 下一步待办

- 等用户确认后，进入 M5：基础 UI。
- M5 处理搜索框、历史卡片、删除、置顶和淡蓝色简洁风格优化。

---

# M5 基础 UI 记录

## 本次目标

- 推进 M5：实现搜索框、历史卡片、删除、置顶和淡蓝色界面优化。
- 保持文字历史可复制回剪贴板。
- 保持本地历史存储。
- 不实现图片记录、保留时间清理、托盘、快捷键或开机自启。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/ui-design.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 扩展文字历史数据结构，增加 `pinned` 字段。
- 兼容旧历史数据，缺少 `pinned` 字段时按未置顶处理。
- 新增置顶和删除 IPC 接口。
- 新增搜索框，支持按文字关键词过滤当前历史。
- 将文字记录调整为卡片展示，并提供复制、置顶、删除操作。
- 置顶记录显示在普通记录之前，同组内按复制时间倒序排列。
- 删除和置顶状态会保存到本地 `text-history.json`。
- 优化淡蓝色界面样式，保持简洁直观。
- 新增 M5 smoke 测试路径，验证搜索、置顶、删除和持久化。

## 验证方式

- 运行 `npm.cmd run build`。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 做 M4 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m5-smoke-test` 验证搜索、置顶、删除和本地持久化。
- 检查源码，确认未实现图片、保留时间清理、托盘、快捷键或开机自启。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 回归 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M5 smoke 测试通过，输出 `M5_SMOKE_TEST_UI_ACTIONS_OK`。

## 遗留问题

- 尚未实现图片记录。
- 尚未实现保留时间清理。
- 尚未实现托盘、快捷键、开机自启。

## 下一步待办

- 等用户确认后，进入 M6：保留时间清理。
- M6 处理 `1 天`、`3 天`、`5 天` 设置，未置顶内容过期清理，置顶内容不自动清理。

---

# M6 保留时间清理记录

## 本次目标

- 推进 M6：实现 `1 天`、`3 天`、`5 天` 保留时长设置。
- 支持未置顶文字记录超过保留时长后自动清理。
- 保持置顶文字记录不因过期被自动清理。
- 不实现图片记录、托盘、快捷键或开机自启。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/ui-design.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 扩展类型定义，新增保留时长设置类型与设置 IPC 接口。
- 新增本地设置读写能力，将保留时长保存到 Electron `userData` 目录下的 `settings.json`。
- 在主进程中增加未置顶过期文字记录清理逻辑。
- 在应用启动、历史更新和保留时长变更时应用清理逻辑。
- 在窗口工具栏增加保留时长选择控件，支持 `1 天`、`3 天`、`5 天`。
- 新增 M6 smoke 测试路径，验证过期未置顶记录被清理、过期置顶记录保留、保留时长设置持久化。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run build`。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 做 M4 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m5-smoke-test` 做 M5 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m6-smoke-test` 验证保留时长设置、过期清理、置顶保留和持久化。
- 检查源码，确认未实现图片记录、托盘、快捷键或开机自启。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 回归 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M5 回归 smoke 测试通过，输出 `M5_SMOKE_TEST_UI_ACTIONS_OK`。
- M6 smoke 测试通过，输出 `M6_SMOKE_TEST_RETENTION_OK`。

## 遗留问题

- 尚未实现图片记录。
- 尚未实现托盘、快捷键、开机自启。
- 本地文字历史和设置当前为明文 JSON，符合第一版已确认范围，但不提供加密。

## 下一步待办

- 等用户确认后，进入 M7：图片记录。
- M7 处理图片复制记录、图片缩略图展示、点击图片记录后重新写入剪贴板，以及单张图片 `10MB` 限制。

---

# M7 图片记录

## 本次目标

- 推进 M7：实现图片复制记录。
- 支持图片缩略图展示。
- 支持点击图片记录后重新写回系统剪贴板。
- 支持单张图片 `10MB` 保存上限。
- 不实现托盘、快捷键或开机自启。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/ui-design.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 将历史记录类型扩展为文字和图片两种记录，并兼容旧文字历史数据。
- 将历史列表、置顶、删除、保留时间清理和持久化逻辑统一到同一历史记录数据流。
- 在主进程中增加图片剪贴板读取、图片去重、图片写回剪贴板和 `10MB` 上限判断。
- 图片以 PNG data URL 字符串保存到本地历史 JSON。
- 在界面中增加图片缩略图卡片展示。
- 保留文字搜索逻辑：搜索关键词时只匹配文字记录，未搜索时显示文字和图片记录。
- 新增 M7 smoke 测试路径，验证图片历史、缩略图、点击写回剪贴板、持久化和大小限制。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run build`。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 做 M4 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m5-smoke-test` 做 M5 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m6-smoke-test` 做 M6 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m7-smoke-test` 验证图片历史、缩略图、写回剪贴板、持久化和大小限制。
- 检查源码，确认未实现托盘、快捷键或开机自启。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 回归 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M5 回归 smoke 测试通过，输出 `M5_SMOKE_TEST_UI_ACTIONS_OK`。
- M6 回归 smoke 测试通过，输出 `M6_SMOKE_TEST_RETENTION_OK`。
- M7 smoke 测试通过，输出 `M7_SMOKE_TEST_IMAGE_HISTORY_OK`。
- 部分 Electron smoke 测试退出前出现 GPU 日志，但退出码为 `0` 且目标 smoke 输出正常。

## 遗留问题

- 尚未实现托盘、快捷键、开机自启。
- 尚未完成 Windows 打包与完整验收。
- 图片当前以内嵌 PNG data URL 保存在本地 JSON 中，符合 M7 最小实现，但大图会增加 JSON 文件体积。
- M7 smoke 测试直接注入图片 data URL 来验证应用图片历史逻辑和写回剪贴板；实际剪贴板轮询仍使用 Electron `clipboard.readImage()`。

## 下一步待办

- 等用户确认后，进入 M8：托盘、快捷键、开机自启。
- M8 处理系统托盘常驻、`Ctrl+Shift+V` 呼出历史面板和开机自启。

---

# M8 托盘、快捷键、开机自启

## 本次目标

- 推进 M8：实现系统托盘常驻。
- 支持关闭主窗口后隐藏到后台继续运行。
- 支持 `Ctrl+Shift+V` 全局快捷键呼出历史面板。
- 支持开机自启设置。
- 不处理打包分发。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/ui-design.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 扩展设置结构，新增 `launchAtLogin` 开机自启字段，并兼容旧设置文件。
- 在界面工具栏新增开机自启开关和 `Ctrl+Shift+V` 快捷键展示。
- 在主进程中新增系统托盘图标、托盘打开菜单和退出菜单。
- 调整窗口关闭行为：非测试和非退出场景下关闭主窗口会隐藏到后台。
- 新增 `Ctrl+Shift+V` 全局快捷键注册，用于显示并聚焦主窗口。
- 新增 Electron 登录项设置调用，使开机自启设置生效。
- 新增 M8 smoke 测试路径，验证托盘、快捷键、开机自启设置、关闭隐藏和窗口恢复。
- 修复 smoke 临时 `userData` 目录在 Windows 文件锁下清理失败会弹主进程错误的问题，改为容错警告。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run build`。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 做 M4 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m5-smoke-test` 做 M5 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m6-smoke-test` 做 M6 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m7-smoke-test` 做 M7 回归验证。
- 运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m8-smoke-test` 验证托盘、快捷键、开机自启设置、关闭隐藏和窗口恢复。
- 检查源码，确认未处理打包分发。

## 验证结果

- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 回归 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M5 回归 smoke 测试通过，输出 `M5_SMOKE_TEST_UI_ACTIONS_OK`。
- M6 回归 smoke 测试通过，输出 `M6_SMOKE_TEST_RETENTION_OK`。
- M7 回归 smoke 测试通过，输出 `M7_SMOKE_TEST_IMAGE_HISTORY_OK`。
- M8 smoke 测试通过，输出 `M8_SMOKE_TEST_TRAY_SHORTCUT_LOGIN_OK`。
- Windows 偶发锁定 smoke 临时 `userData` 目录时，会输出 `Smoke userData cleanup skipped: EPERM...` 警告，但进程退出码为 `0`，不再弹出主进程错误窗口。

## 遗留问题

- 尚未完成 Windows 打包与完整验收。
- smoke 临时目录在 Windows 文件锁下可能残留，需要后续打包/验收阶段观察是否需要延迟清理策略。
- 开机自启是否在最终安装包路径下稳定生效，需要 M9 打包后再次验证。

## 下一步待办

- 等用户确认后，进入 M9：打包与验收。
- M9 处理 Windows 可运行产物生成和最终验收检查。

---

# M9 打包与验收

## 本次目标

- 推进 M9：生成 Windows 可运行产物。
- 按测试验收文档做最终检查。
- 不新增业务功能。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 确认当前项目尚未配置打包工具，且 `node_modules/electron/dist` 已存在 Electron runtime。
- 新增 `scripts/package-win.js`，使用 Node 标准库复制 Electron runtime、`dist`、`src` 和 `package.json`。
- 新增 `npm.cmd run package:win` 打包命令。
- 生成 Windows unpacked 可运行产物：`release/历史剪贴板-win32-x64/历史剪贴板.exe`。
- 验证打包产物目录包含 Electron runtime、`resources/app/dist`、`resources/app/src` 和 `resources/app/package.json`。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/development-steps.md` 和 `docs/testing-acceptance.md`。

## 验证方式

- 运行 `npm.cmd run package:win`。
- 检查 `release/历史剪贴板-win32-x64/历史剪贴板.exe` 存在。
- 运行打包产物 `$env:ELECTRON_RUN_AS_NODE=$null; & "C:\Users\16041\Desktop\1\release\历史剪贴板-win32-x64\历史剪贴板.exe" --smoke-test`。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m3-smoke-test` 做 M3 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m4-smoke-test` 做 M4 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m5-smoke-test` 做 M5 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m6-smoke-test` 做 M6 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m7-smoke-test` 做 M7 回归验证。
- 顺序运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --m8-smoke-test` 做 M8 回归验证。
- 运行 `npm.cmd run build` 做最终编译验证。

## 验证结果

- `npm.cmd run package:win` 通过，输出 `PACKAGED C:\Users\16041\Desktop\1\release\历史剪贴板-win32-x64`。
- `release/历史剪贴板-win32-x64/历史剪贴板.exe` 存在，文件大小约 `232MB`。
- 打包产物 smoke 测试退出码为 `0`；Windows GUI 可执行文件未将 `console.log` 透回当前终端，因此没有显示 smoke 文本输出。
- `npm.cmd run build` 编译通过。
- M3 回归 smoke 测试通过，输出 `M3_SMOKE_TEST_TEXT_HISTORY_OK`。
- M4 回归 smoke 测试通过，输出 `M4_SMOKE_TEST_PERSISTENCE_OK`。
- M5 回归 smoke 测试通过，输出 `M5_SMOKE_TEST_UI_ACTIONS_OK`。
- M6 回归 smoke 测试通过，输出 `M6_SMOKE_TEST_RETENTION_OK`。
- M7 回归 smoke 测试通过，输出 `M7_SMOKE_TEST_IMAGE_HISTORY_OK`。
- M8 回归 smoke 测试通过，输出 `M8_SMOKE_TEST_TRAY_SHORTCUT_LOGIN_OK`。
- Windows 偶发锁定 smoke 临时 `userData` 目录时，会输出 `Smoke userData cleanup skipped: EPERM...` 警告，但对应 smoke 退出码为 `0`。

## 遗留问题

- 当前产物为未安装版文件夹，不包含安装器、代码签名或自动更新。
- 开机自启在最终安装器路径下的行为尚未验证，因为当前 M9 不生成安装器。
- smoke 临时目录在 Windows 文件锁下可能残留，当前已降级为警告。

## 下一步待办

- 第一版核心里程碑 M0-M9 已完成。
- 后续如需安装器、代码签名、自动更新或新功能，需要用户另行确认范围后再推进。
