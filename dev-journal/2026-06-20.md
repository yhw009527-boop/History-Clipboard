# 2026-06-20 开发日志

## 本次目标

- 完成 M0：文档与工作规范初始化。
- 只创建项目管理文档，不写应用功能代码。

## 完成事项

- 创建 `docs/` 目录，用于存放项目需求、技术方案、UI 规范、开发步骤和测试验收标准。
- 创建 `dev-journal/` 目录，用于记录每日开发事项和待办事项。
- 创建 `CLAUDE.md`，写入工作前阅读路径、工作结束日志要求和里程碑推进规则。
- 记录当前阶段为 M0，下一阶段为 M1 技术路线确认。

## 验证方式

- 检查 `docs/`、`dev-journal/`、`CLAUDE.md` 是否存在。
- 检查 M0 所需 Markdown 文件是否完整。

## 验证结果

- M0 所需目录和文件已创建完成。
- `docs/`、`dev-journal/`、`CLAUDE.md` 均已存在。
- `docs/requirements.md`、`docs/technical-plan.md`、`docs/ui-design.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 均已存在。
- `dev-journal/README.md`、`dev-journal/_template.md`、`dev-journal/2026-06-20.md` 均已存在。

## 遗留问题

- 技术栈尚未锁定。
- M1 需要对 Electron、.NET 桌面方案做简要对比，并由用户确认第一版技术路线。

## 下一步待办

- 等用户确认后，进入 M1：技术路线确认。
- 在 M1 中更新 `docs/technical-plan.md`。

## 备注

- “每天自动记录”当前采用工作规范强制记录，不配置系统定时任务。

---

# M1 技术路线确认记录

## 本次目标

- 推进 M1：对 Electron、.NET 桌面和 Tauri 路线做简要对比。
- 根据项目需求和本机环境，给出第一版技术路线建议。
- 不创建应用项目骨架，不写应用功能代码。

## 完成事项

- 阅读 `CLAUDE.md`、`docs/requirements.md`、`docs/technical-plan.md`、`docs/development-steps.md`、`docs/testing-acceptance.md` 和 `dev-journal/README.md`。
- 检查本机工具链，确认 Node.js、npm、.NET 运行时、.NET SDK 和 Rust 的可用状态。
- 查询 Electron 和 Microsoft 官方文档，确认剪贴板、托盘、全局快捷键、开机自启和 Windows 桌面框架相关依据。
- 更新 `docs/technical-plan.md`，记录 M1 推荐结论：第一版建议采用 Electron。
- 更新 `docs/testing-acceptance.md`，补充 M1 验收标准。
- 更新 `docs/requirements.md` 和 `CLAUDE.md`，同步当前技术路线状态。

## 验证方式

- 使用 `node --version` 检查 Node.js。
- 使用 `npm.cmd --version` 检查 npm。
- 使用 `dotnet --list-sdks` 和 `dotnet --list-runtimes` 检查 .NET 环境。
- 使用 `Get-Command rustc` 检查 Rust。
- 检查相关 Markdown 文件是否包含 M1 更新内容。

## 验证结果

- Node.js 可用：`v24.14.1`。
- npm 可用：`npm.cmd 11.11.0`。
- PowerShell 直接执行 `npm` 会被执行策略拦截，后续应使用 `npm.cmd`。
- .NET SDK 未检测到。
- .NET 运行时存在。
- Rust 未检测到。
- M1 技术方案文档已更新，推荐 Electron 作为第一版技术路线。

## 遗留问题

- Electron 路线仍需用户确认后，才能进入 M2 项目骨架。
- 打包形式尚未最终确定。
- 数据存储格式尚未最终确定。

## 下一步待办

- 等用户确认是否采用 Electron。
- 用户确认后，再进入 M2：创建最小可运行桌面应用骨架。

---

# M2 项目骨架记录

## 本次目标

- 推进 M2：创建最小可运行 Electron 桌面应用骨架。
- 只验证窗口能打开、项目能启动。
- 不实现剪贴板监听、本地存储、托盘、快捷键或开机自启。

## 完成事项

- 创建 `.gitignore`。
- 创建 `package.json`，包含 `build`、`start`、`smoke` 脚本。
- 创建 `tsconfig.json`。
- 创建 `src/main.ts`，用于创建最小 Electron 主窗口。
- 创建 `src/preload.ts`。
- 创建 `src/renderer.ts`。
- 创建 `src/index.html`。
- 创建 `src/styles.css`。
- 安装 Electron、TypeScript 和 Node 类型依赖。
- 修正 TypeScript 配置为 `Node16` 模块解析。
- 修正启动脚本，避免 `ELECTRON_RUN_AS_NODE=1` 影响 Electron 主进程。
- 更新 `CLAUDE.md`、`docs/technical-plan.md`、`docs/requirements.md`、`docs/testing-acceptance.md` 和 `docs/development-steps.md`。

## 验证方式

- 运行 `npm.cmd install` 安装依赖。
- 运行 `npm.cmd run build` 编译 TypeScript。
- 检查 `node_modules/electron/dist/electron.exe` 是否存在。
- 在允许 GUI 的环境中运行 `$env:ELECTRON_RUN_AS_NODE=$null; .\node_modules\.bin\electron.cmd . --smoke-test`。

## 验证结果

- 依赖安装成功，npm 审计结果为 `found 0 vulnerabilities`。
- `npm.cmd run build` 编译通过。
- Electron 二进制已安装，`node_modules/electron/dist/electron.exe` 存在。
- smoke 测试通过，输出 `M2_SMOKE_TEST_WINDOW_LOADED`。
- 沙箱内直接启动 Electron GUI 会失败；在沙箱外授权运行后验证通过。

## 遗留问题

- 打包形式尚未确定。
- 数据存储格式尚未确定。
- 当前未实现任何业务功能。
- M3 需要实现文字剪贴板监听、展示和重新写回剪贴板。

## 下一步待办

- 等用户确认后，进入 M3：文字剪贴板监听。
- M3 不实现图片记录、本地持久化、置顶、删除、搜索、托盘、快捷键或开机自启。
